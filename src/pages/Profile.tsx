import React from 'react';

export const Profile: React.FC = () => {
    return (
        <div className="px-xl py-xl text-center">
            <h2 className="text-[1.75rem] font-bold text-text-primary">User Profile (Work in Progress)</h2>
            <p className="text-text-secondary">User Info, Settings, and Logout follow here.</p>
        </div>
    );
};
